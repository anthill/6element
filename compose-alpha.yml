api:
   build: ./dockerfiles
   dockerfile: node
   command: node server/index.js
   links:
      - db
   ports:
      - "8001:8001"
   environment:
      - VIRTUAL_HOST=6element-alpha.ants.builders
      - VIRTUAL_PORT=8001
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - PGPASSWORD=postgres
      - NODE_ENV=alpha
   volumes:
      - ./:/6element
      - ./backups:/backups/
   tty: true

db:
   build: ./dockerfiles
   dockerfile: postgis
   environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
   tty: true
