{
  "name": "tests",
  "version": "0.0.0",
  "description": "Unitary tests for 6element",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ants <contact@ants.builders> (http://ants.builders/)",
  "license": "ISC",
  "dependencies": {
    "request": "^2.58.0"
  }
}
